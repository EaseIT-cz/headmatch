# HeadMatch Backlog

> **Moved.** The active backlog now lives at [`/BACKLOG.md`](../BACKLOG.md) (repo
> root), in the vornik autonomy `backlog` v1 marker format — the vornik daemon
> reads it directly and works one `- [ ]` item per iteration into a draft PR.
>
> - `- [ ]` ready · `- [?]` proposed (promote to work it) · `- [x]` done · `- [!]` failed
>
> Capabilities and per-release history live in `CHANGELOG.md` and the release
> notes under `docs/releases/`.
